{
  "name": "mcp-pin",
  "version": "0.1.0",
  "description": "Tool-integrity pinning for MCP. Pin what your agent approved; block it when it changes.",
  "bin": { "mcp-pin": "bin/attest.js" },
  "files": ["bin", "src", "README.md"],
  "engines": { "node": ">=20" },
  "license": "MIT",
  "keywords": ["mcp", "model-context-protocol", "security", "tool-poisoning", "supply-chain"]
}
